#include "main.h"
/**
 * add- add two numbers
 *@x: the frist pram
 *@y: the second pram
 *
 * Return: the addation of two numbers
 *
 */
int add(int x, int y)
{
	int sum;

	sum = x + y;
	return (sum);
}
